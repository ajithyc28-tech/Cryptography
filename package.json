{
  "name": "memcrypt-lab",
  "version": "1.0.0",
  "description": "MemCrypt Lab — Attack Lab & Cryptographic Engine",
  "scripts": {
    "start": "npx live-server --port=3000 --open=index.html",
    "serve": "npx http-server -p 3000 -o"
  },
  "devDependencies": {
    "live-server": "^1.2.2",
    "http-server": "^14.1.1"
  }
}
